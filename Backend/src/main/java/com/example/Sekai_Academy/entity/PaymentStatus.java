package com.example.Sekai_Academy.entity;

// Enum definition (if not already defined)
public enum PaymentStatus {
    PENDING,   // Payment is in process
    COMPLETED, // Payment successfully processed
    FAILED     // Payment unsuccessful
}
